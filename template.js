// Function to create a multi-line string with template literals
function createGreeting(name, age) {
    return `Hello, my name is ${name}.
  I am ${age} years old.`;
  }
  
  // Example usage:
  console.log(createGreeting('John', 30));
  